# Port targets — `bm25-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `bm25` that codex-rs uses, and therefore the minimum subset that `bm25-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-25] (rank `211`, tier `direct`, status `To Do`)

**Cargo package name**: `bm25`
**Rust module name (in `use` statements)**: `bm25`

## Summary

- **4** distinct symbols imported by codex-rs
- **4** total `use` statements
- **1** codex source files affected
- **1** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `bm25::Document`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-core`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/tools/handlers/tool_search.rs:8` (`codex-core`) — `use bm25::Document`

### `bm25::Language`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-core`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/tools/handlers/tool_search.rs:9` (`codex-core`) — `use bm25::Language`

### `bm25::SearchEngine`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-core`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/tools/handlers/tool_search.rs:10` (`codex-core`) — `use bm25::SearchEngine`

### `bm25::SearchEngineBuilder`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-core`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/tools/handlers/tool_search.rs:11` (`codex-core`) — `use bm25::SearchEngineBuilder`
